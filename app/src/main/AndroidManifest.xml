<manifest xmlns:android="http://schemas.android.com/apk/res/android"

    package="br.com.tutorialdb">

    <application
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name">

        <activity
            android:name=".controlador.MainController"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>


        <activity android:name=".controlador.BuscarCarroController" />
        <activity android:name=".controlador.AtualizarCarroController" />
        <activity android:name=".controlador.ExcluirCarroController" />

    </application>
</manifest>
